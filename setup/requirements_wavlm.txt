# WavLM call extractor (EC2 runtime)
#
# IMPORTANT:
# - Install a CUDA-enabled `torch`/`torchaudio` build separately (depends on AMI/CUDA).
# - Keep these pins aligned with the codebase and capture `pip freeze` per run in S3 artifacts.

transformers==4.49.0
accelerate==1.5.2
datasets==3.3.2
soundfile==0.13.1
boto3==1.34.0
numpy==1.26.4
tqdm==4.67.1
safetensors==0.5.3
scikit-learn==1.5.2
