# Core pipeline dependencies
boto3>=1.34.0
numpy>=1.24.0
scikit-learn>=1.3.0
pydantic>=2.0.0

# CLI tools
textual>=0.89.0
typer>=0.9.0

# Testing
pytest>=7.0.0

# GenRM workbench
streamlit>=1.30.0

# Azure/OpenAI client (synthetic system prompt generation)
openai>=1.40.0

# Gemini pilot
google-genai>=1.57.0

# Note: WhisperX pipeline has additional heavy dependencies (torch, whisperx, etc.)
# that should be installed in Docker container - see docker/whisperx/requirements.txt
